# Optional: required only when you want to run the local Gemini metering proxy.
GEMINI_API_KEY=

# Optional: only needed when geminiBackend is "relay".
RELAY_BASE_URL=
RELAY_KEY=

# Optional: launchd proxy used by scripts/install.sh when generating the agent.
HTTPS_PROXY=
